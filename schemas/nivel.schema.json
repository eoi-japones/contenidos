{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "id": {

            "type": "string",
            "description": "Identificador del nivel (único en todo el sistema)"
        },

        "clave": {

            "type": "string",
            "description": "la clave asignada al nivel"
        }

    },

    "additionalProperties": false,

    "required": ["id", "clave"]
}
